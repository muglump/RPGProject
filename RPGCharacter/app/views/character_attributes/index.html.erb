<h1>Listing character_attributes</h1>

<table>
  <tr>
    <th>Charid</th>
    <th>Attrid</th>
    <th>Value</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @character_attributes.each do |character_attribute| %>
  <tr>
    <td><%= character_attribute.charID %></td>
    <td><%= character_attribute.attrID %></td>
    <td><%= character_attribute.value %></td>
    <td><%= link_to 'Show', character_attribute %></td>
    <td><%= link_to 'Edit', edit_character_attribute_path(character_attribute) %></td>
    <td><%= link_to 'Destroy', character_attribute, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Character attribute', new_character_attribute_path %>
