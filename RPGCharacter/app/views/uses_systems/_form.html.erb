<%= form_for(@uses_system) do |f| %>
  <% if @uses_system.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@uses_system.errors.count, "error") %> prohibited this uses_system from being saved:</h2>

      <ul>
      <% @uses_system.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :userID %><br />
    <%= f.number_field :userID %>
  </div>
  <div class="field">
    <%= f.label :systemId %><br />
    <%= f.number_field :systemId %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
