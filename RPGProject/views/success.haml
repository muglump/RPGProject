!!! 5

%html
  %head
    %title RPG Character Database
    %link{:rel => "stylesheet", :type => "text/css", :href => url("style.css")}
 
  %body 
    - rc = locals[:return_code]
    - if rc == 0
      %div Successful account creation
    - else
      %div Failed to create account.
      - if rc == 1
        %div User already in database